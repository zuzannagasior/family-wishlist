{
  "name": "family-wishlist-backend",
  "version": "1.0.0",
  "description": "family wishlist application backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "engines": {
    "node": "12.13.1"
  },
  "author": "Zuzanna Gasior",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.8.11"
  }
}
